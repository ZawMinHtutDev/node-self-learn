<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cateGoryName.charAt(0).toUpperCase() + cateGoryName.slice(1, cateGoryName.length); %></title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            background-color: skyblue;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }
        h1, h2 {
            color: #fff;
            font-weight: 700;
            text-transform: uppercase;
            text-align: center;
        }
        h2 {
            color: #eee;
        }
        * {
            color: #ddd;
            font-weight: 500;
            text-align: center;
        }
        ul {
            padding: 0;
        }
        li {
            list-style: none;
            margin-bottom: 20px;
            font-weight: 500;
            text-transform: capitalize;
        }
    </style>
</head>
<body>
    <body>
        <h1>Welcome to the category page of <%= cateGoryName %></h1>
        <h2>Available products</h2>
        <ul>
            <% products.forEach(product => { %>
                <li><%= product %></li>
            <% }) %>
        </ul>
    </body>
</body>
</html>